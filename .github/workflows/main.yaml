name: main
on:
  push:
    branches: [ main ]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  test:
    uses: ./jobs/test.yaml
  rekease:
    needs: test
    uses: ./jobs/release.yaml
